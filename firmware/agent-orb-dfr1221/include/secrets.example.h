#pragma once

// 复制为 secrets.h 后填写。secrets.h 已被 .gitignore 忽略。
#define ORB_WIFI_SSID "your-wifi-name"
#define ORB_WIFI_PASSWORD "your-wifi-password"

// 填写运行 Gateway 的电脑局域网 IP，不要使用 localhost。
#define ORB_GATEWAY_URL "http://192.168.1.100:8787"
#define ORB_DEVICE_ID "desk-orb"

// 运行 scripts/provision_gateway_token.py 自动生成 gateway_token.h，不要手写或提交 Token。
